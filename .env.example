SUPABASE_URL=
SUPABASE_ANON_KEY=

# Holder access stays closed until every value is configured.
HOLDER_ACCESS_ENABLED=false
SWIM_TOKEN_MINT=
# Positive integer in raw token units (not display tokens). No default threshold.
SWIM_MINIMUM_RAW_BALANCE=
# Server-only Solana mainnet RPC endpoint; never expose a private RPC key to the client.
SOLANA_RPC_URL=
